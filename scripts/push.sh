# scripts/push.sh (optional, used by the old workflow; new pushes are embedded in YAML)
#!/usr/bin/env bash
set -e
# This script is no longer called by the workflow; keeping for compatibility.
echo "Use the embedded push steps instead."
